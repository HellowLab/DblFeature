import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  bottomSection: {
    padding: 20,
    borderTopWidth: 1,
    borderColor: "#ccc",
  },
  versionText: {
    marginTop: 10,
    textAlign: "center",
    color: "#777",
  },
});
